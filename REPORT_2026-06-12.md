# 2026-06-12 報告：obs v3 重訓潮收官 → 蒸餾單一網 → 縫合怪零樣本測試

目標（/goal）：訓練模型 → 蒸餾 → 生成三隻縫合怪做泛化性測試並觀察行為。三段全完成。

## 1. 重訓潮（11 席，配方 = bm 試點：train_team_ppo --asym 40u，原生 v3）

**9/12 席換上原生 v3 檔，官方 120g 重驗 12/12 GOAL MET**
（eval_results/goal_1v1_after_v3_wave_120g.txt；逐席 gate 紀錄 eval_results/gate_*.log）。

- 真提升（超 2σ 帶）：**arcane_trickster +7.3pp**（41.4 vs 34.1，超專家席位）、
  **evocation +4.9pp**（75.5 vs 70.6，generalist 分家）、**assassin +4.5pp**（追平專家席位）。
- par-swap（同帶換檔，吃進等級敏感 critic）：champion/totem_bear/divination/vengeance/war。
- 全部換檔 critic 等級條件化出現：V(s) spread 0.00 → 1.4~9.3（盜賊雙席最強）。
- **3 個 BC 席位（berserker/devotion/life）重訓挑戰全敗，維持現役**——新收示範的每個
  epoch 在 pick 都輸給現役檔。in-context BC 檔=對示範分佈過擬合的精品，重訓不保證複現。
- devotion probe spread 0.617「異常」查證：是對手開場行為的等級門檻（L3+ 開聖武器、
  legacy 可見 status 位），不是 v3 洩漏。probe 文檔已加 caveat。

## 2. 蒸餾：12 個老師 → 1 個 shared-head 網（scripts/distill_routed.py）

student = `CombatPolicyNet(n_head_groups=1)`（蒸餾是固定目標監督學習，per-arch heads
的梯度衝突前提不存在）。A/B 對照同資料：A 保留身分輸入；B `--blind` 把 self one-hot +
end_features arch 尾歸零（純能力條件化）。

| student | mean diff | 過帶 | SHORT |
|---|---|---|---|
| A（身分） | +1.9% | 8/12 | champ −10、assassin −14、AT −9、devotion −21 |
| **B（盲身分）** | **+3.8%** | **11/12** | champion −9 |

**核心發現：盲掉身分反而更強**——身分通道讓網把容量切成 12 條窄路；被迫從能力特徵
學習後，結構跨職業共享，稀缺職業全數回帶。

對照實驗（資料 31.3k=2.3×、16 epochs）：
- 16 epochs 過擬合（B 同 run e08 +3.5 → e16 +0.7；identity-weight 放大稀有招）；
- 資料加倍在固定 epoch 下無增益（+3.5 ≈ +3.8）→ **殘餘差距不是樣本飢餓**，
  是 greedy 老師軌跡的 covariate shift（偏離態無覆蓋、精密序列職業最受傷）。
  下一個正確槓桿是 DAgger（老師當 oracle 標學生軌跡的偏離態），未執行。

冠軍檔：`models/distill_b/distill_e08.pt`（評測需帶 --blind）。

## 3. 縫合怪零樣本測試（chimera_defs.py 三隻；eval_chimera.py，120 場/隻/網）

縫合怪 = 運行時註冊的 ClassDef（職業資料化重構的直接受益），one-hot 全零＝怪物條件。
報告檔 eval_results/chimera_{a,b}_e08.txt（含逐回合 trace）。

| 縫合怪 | A | B | 行為觀察（B） |
|---|---|---|---|
| omni（狂暴+治療+火球+瞬移+定身+surge） | 54% | **56%** | 自發明 rage(bonus)+fireball(action) 回合循環——無老師示範過；但 bonus 被 rage 壟斷→治療飢餓（0.02/場）；對 totem_bear 站樁 25 輪丟火球不收頭 |
| gish（重甲聖騎面板+法師輸出） | 31% | **52%** | B 連貫：火球開場→近戰→29% HP 才聖療；A 退化：90% HP 狂點聖療 1.77/場、wasted move 5.15/場 |
| trickhealer（盜賊底盤+牧師支援） | 13% | **21%** | 兩者都塌縮到最近老師（assassin 劇本 hide+短弓循環），牧師套件全程 0 使用 |

**結論**：
1. 零件層遷移成立（單招時機正確、合法），組合層部分湧現（omni 的雙資源循環）；
2. 遠離所有老師的組合會**塌縮到最近原型**——這是 population training 必須把「隨機合成
   身分」放進取樣池的實證理由：讓組合本身成為分佈內事件；
3. A 對 OOD one-hot 直接退化，B 天然免疫——**怪物/新職業路線上，能力條件化不是
   選項而是前提**。

## 下一步（依數據排序）
1. DAgger 修蒸餾的 covariate shift（champion 等精密職業的殘差來源）；
2. population training 取樣池加入隨機合成身分（治塌縮）；
3. 天賦→可觀測特徵編碼（sculpt 的 ally_safe 位等），徹底取代 one-hot 的最後資訊角色。
